import type { ContentPost } from '@/lib/content-types';

// Tools content — no dedicated type yet; uses ContentPost union.
// As posts are added, import them here and add to the array.
// Example:
// import semrushReview from './semrush-review';
// export const tools: ContentPost[] = [semrushReview];

export const tools: ContentPost[] = [];
