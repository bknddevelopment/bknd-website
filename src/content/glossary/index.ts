import type { GlossaryTerm } from '@/lib/content-types';

// As terms are added, import them here and add to the array.
// Example:
// import seoTerm from './seo';
// export const glossaryTerms: GlossaryTerm[] = [seoTerm];

export const glossaryTerms: GlossaryTerm[] = [];
